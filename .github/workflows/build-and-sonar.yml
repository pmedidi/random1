# name: Build and Analyze with SonarQube

# on:
#   push:
#     branches:
#       - master

# jobs:
#   build-and-analyze:
#     name: Build and Analyze
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3
#         with:
#           fetch-depth: 0

#       - name: Set up JDK 17
#         uses: actions/setup-java@v3
#         with:
#           distribution: 'adopt'
#           java-version: '17'

#       - name: Run SonarQube Scan for group_1_SOLID_GRASP
#         uses: sonarsource/sonarqube-scan-action@master
#         with:
#           projectBaseDir: group_1_SOLID_GRASP
#         env:
#           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
#           SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
name: Build

on:
  push:
    branches:
      - main

jobs:
  build-group-1:
    name: Build Group 1
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis

      - uses: actions/setup-java@v2
        with:
          distribution: 'temurin'
          java-version: 17

      - name: SonarQube Scan Group 1
        uses: sonarsource/sonarqube-scan-action@master
        with:
          projectBaseDir: group_1_SOLID_GRASP
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

  build-group-2:
    name: Build Group 2
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis

      - uses: actions/setup-java@v2
        with:
          distribution: 'temurin'
          java-version: 17

      - name: SonarQube Scan Group 2
        uses: sonarsource/sonarqube-scan-action@master
        with:
          projectBaseDir: group_2_SOLID_GRASP
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
